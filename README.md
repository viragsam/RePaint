# What it is:
A local, no-install tool that lets you live-edit the fonts and colors of any loaded webpage, your own dev server, localhost, or a live site exatly the way you'd fiddle with values in dev tools, but through a proper side panel instead of hand-editing the DOM.
